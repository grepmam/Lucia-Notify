#!/usr/bin/perl

use strict;
use warnings;

use lib '../lib';
use Lucia::Lucia;

my $lucia = Lucia::Lucia->new;
$lucia->set_lang('en');
$lucia->set_time(10);
$lucia->active_sound(0);
$lucia->active_voice(0);
$lucia->active_no_greeting(0);
$lucia->active_debug(1);

#$lucia->simulate( '1732853244' );
#$lucia->notify_for_bugs( '17413' );
$lucia->notify_for_user( 'mmartel' );
